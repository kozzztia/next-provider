import Title from '@/ui-kit/Title';
import React from 'react';

interface Props {
}

const Todos = (props: Props) => {
    return (
        <div>
            <Title text="todos"/>
        </div>
    );
};

export default Todos;
